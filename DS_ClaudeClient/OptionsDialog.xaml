<Window x:Class="DS_ClaudeClient.OptionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Options"
        Height="720" Width="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Border Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0"
                    Background="{StaticResource BackgroundBrush}"
                    CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="Options"
                               Foreground="{StaticResource TextBrush}"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               FontSize="13"/>
                    <Button HorizontalAlignment="Right"
                            Style="{StaticResource IconButtonStyle}"
                            Click="CloseButton_Click"
                            Margin="0,0,4,0">
                        <TextBlock Text="&#x2715;" FontSize="14"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20,20,20,10">
                <!-- Font Family -->
                <TextBlock Text="Font Family"
                           Foreground="{StaticResource TextBrush}"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"/>
                <ComboBox x:Name="FontFamilyComboBox"
                          SelectionChanged="FontFamilyComboBox_SelectionChanged"
                          Padding="8,6"
                          Margin="0,0,0,16"/>

                <!-- Font Size -->
                <TextBlock Text="Font Size"
                           Foreground="{StaticResource TextBrush}"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>
                    <Slider x:Name="FontSizeSlider"
                            Minimum="10" Maximum="24"
                            Value="14"
                            TickFrequency="1"
                            IsSnapToTickEnabled="True"
                            ValueChanged="FontSizeSlider_ValueChanged"/>
                    <TextBlock x:Name="FontSizeLabel"
                               Grid.Column="1"
                               Text="14px"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right"/>
                </Grid>

                <!-- Text Area Font Family -->
                <TextBlock Text="Text Area Font"
                           Foreground="{StaticResource TextBrush}"
                           FontWeight="SemiBold"
                           Margin="0,16,0,8"/>
                <ComboBox x:Name="TextAreaFontFamilyComboBox"
                          SelectionChanged="TextAreaFontFamilyComboBox_SelectionChanged"
                          Padding="8,6"
                          Margin="0,0,0,16"/>

                <!-- Text Area Font Size -->
                <TextBlock Text="Text Area Font Size"
                           Foreground="{StaticResource TextBrush}"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>
                    <Slider x:Name="TextAreaFontSizeSlider"
                            Minimum="10" Maximum="24"
                            Value="14"
                            TickFrequency="1"
                            IsSnapToTickEnabled="True"
                            ValueChanged="TextAreaFontSizeSlider_ValueChanged"/>
                    <TextBlock x:Name="TextAreaFontSizeLabel"
                               Grid.Column="1"
                               Text="14px"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right"/>
                </Grid>

                <!-- Send Key Combination -->
                <TextBlock Text="Send Message Shortcut"
                           Foreground="{StaticResource TextBrush}"
                           FontWeight="SemiBold"
                           Margin="0,16,0,8"/>
                <ComboBox x:Name="SendKeyComboBox"
                          SelectionChanged="SendKeyComboBox_SelectionChanged"
                          Padding="8,6"/>

                <!-- Snippets Storage Location -->
                <TextBlock Text="Snippets Storage Location"
                           Foreground="{StaticResource TextBrush}"
                           FontWeight="SemiBold"
                           Margin="0,16,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="SnippetsFilePathTextBox"
                             Grid.Column="0"
                             Padding="8,6"
                             VerticalAlignment="Center"
                             Style="{StaticResource SearchTextBoxStyle}"/>
                    <Button Grid.Column="1"
                            Content="Browse..."
                            Style="{StaticResource DialogButtonStyle}"
                            Click="BrowseSnippetsPath_Click"
                            Margin="8,0,0,0"
                            Padding="12,6"/>
                </Grid>
                <TextBlock Text="Default: OneDrive\ds_snippets.json"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="11"
                           Margin="0,4,0,0"/>

                <!-- Show Log Button -->
                <Button Content="Show Snippets Log"
                        Style="{StaticResource DialogButtonStyle}"
                        Click="ShowLogButton_Click"
                        HorizontalAlignment="Left"
                        Margin="0,16,0,0"
                        Padding="12,8"/>
            </StackPanel>
            </ScrollViewer>

            <!-- Buttons -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="20,10,20,20">
                <Button Content="Cancel"
                        Style="{StaticResource DialogButtonStyle}"
                        Click="CancelButton_Click"
                        Margin="0,0,10,0"/>
                <Button Content="Save"
                        Style="{StaticResource PrimaryDialogButtonStyle}"
                        Click="SaveButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
